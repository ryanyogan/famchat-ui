import { email, password, passwordConfirmation } from 'famchat/utils/user-validation';
import { module, test } from 'qunit';

module('Unit | Utility | user validation');

// Replace this with your real tests.
test('it works', (assert) => {
  assert.ok(email);
  assert.ok(password);
  assert.ok(passwordConfirmation);
});
